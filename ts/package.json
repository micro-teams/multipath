{
  "name": "@micro-teams/multipath",
  "version": "0.2.0",
  "description": "Redundant multi-path transport for the browser: one reliable, multiplexed byte stream over many network paths to one origin.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/micro-teams/multipath.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "lint": "oxlint src test"
  },
  "devDependencies": {
    "oxlint": "^0.15.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
