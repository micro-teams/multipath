name: multipath
description: >-
  Transport resilience across redundant network paths: line registry, health and ranking, hedged
  reads, idempotent write failover, and stream line selection. No dependencies, no transport.
version: 0.1.6
repository: https://github.com/micro-teams/multipath
issue_tracker: https://github.com/micro-teams/multipath/issues

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
